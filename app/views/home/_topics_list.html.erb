<% topics.each do |t| %>
  <tr>
    <td><%= t.name %></td>
    <td><%= t.category.name %></td>
    <td><%= t.user.email %></td>
    <td><%= t.posts_count %></td>
    <td><%= t.posts_count %></td>
    <td><%= t.created_at %></td>
  </tr>
<% end %>

