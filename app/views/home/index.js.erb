$('#topics_table tbody').append('<%= j(render partial: 'topics_list', locals: { topics: @topics})%>');
<% if @topics.next_page %>
  $('.pagination').replaceWith('<%= j will_paginate(@topics) %>');
<% else %>
  $('.pagination').remove();
  $('#loading_gif').remove();
<% end %>
